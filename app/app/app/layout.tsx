export const metadata = {
  title: 'Pickem',
  description: 'Sports pickem application',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
